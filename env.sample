# Horror Movie Quote Video Generator - Environment Variables
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI Organization ID (if you belong to multiple organizations)
# OPENAI_ORG_ID=your_organization_id_here

# Optional: Custom OpenAI API Base URL (if using a proxy or different endpoint)
# OPENAI_API_BASE=https://api.openai.com/v1

# Optional: Maximum tokens for quote generation (default is usually fine)
# OPENAI_MAX_TOKENS=1000

# Video Generation Settings (optional - can also be set via command line)
# DEFAULT_QUOTES_COUNT=9
# DEFAULT_DURATION_PER_FRAME=10

# Audio Settings (optional)
# DEFAULT_AUDIO_VOLUME=1.0
# PREFERRED_AUDIO_FORMAT=mp3

# YouTube API Settings (only needed if using --upload feature)
# These are handled by the client_secret.json file, but you can set additional options here
# YOUTUBE_PRIVACY_STATUS=private
# YOUTUBE_CATEGORY_ID=17

# Debugging and Logging (optional)
# DEBUG_MODE=false
# LOG_LEVEL=INFO

# Performance Settings (optional)
# MAX_CONCURRENT_REQUESTS=5
# REQUEST_TIMEOUT=30

# File Paths (optional - uses defaults if not set)
# QUOTES_DIR=./quotes
# IMAGES_DIR=./images
# FRAMES_DIR=./frames
# OUTPUT_DIR=./output
# AUDIO_DIR=./audio

# Notes:
# - Never commit the actual .env file to version control
# - Keep your API keys secure and never share them
# - The OPENAI_API_KEY is the only required variable
# - All other settings are optional and have sensible defaults
# - Boolean values should be: true/false (lowercase)
# - Numeric values should be plain numbers without quotes
# - String values with spaces should be quoted: "my value"
