# Horror Movie Quote Video Generator - Requirements
# Install with: pip install -r requirements.txt

# OpenAI API client: quote generation (chat) and image-fallback generation (Images API)
openai>=1.0.0

# Image processing library for compositing text onto generated images
Pillow>=9.0.0

# Environment variable management for API keys
python-dotenv>=0.19.0

# Config file parsing
PyYAML>=6.0

# HTTP client for the ElevenLabs Music API
requests>=2.31.0

# Google authentication libraries for YouTube upload functionality
google-auth>=2.0.0
google-auth-oauthlib>=0.5.0
google-auth-httplib2>=0.1.0

# Google API client for YouTube Data API
google-api-python-client>=2.0.0

# Local Stable Diffusion image generation (Apple Silicon MPS backend)
torch>=2.2.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0

# Local Stable Audio Open music generation (Apple Silicon MPS backend) --
# saving generated waveforms to disk before ffmpeg crossfades/encodes them.
# torchsde is required by the pipeline's default scheduler even though it's
# never imported directly in this codebase.
soundfile>=0.12.0
torchsde>=0.2.5

# Dev / quality tooling
pytest>=7.4.0
pylint>=3.0.0
bandit>=1.7.0

# Note: FFmpeg is also required but must be installed separately
# See INSTALL.md for FFmpeg installation instructions
